# 2026-08-16 开发记录

> 日期：2026-08-16  
> 记录范围：管理端、系统配置、审计日志、限流、测试与安全评审

## 一、今天已完成

### 1. 文档与部署说明

- 在 README 中补充生产部署和 Nginx 反向代理说明
- 在系统框架文档中补充开发代理与生产反向代理说明
- 更新模块任务清单中的部署任务
- 新增安全风险与加固建议文档

### 2. 管理端用户管理

后端：

- 补充 `User` 实体更新方法
- 新增 `AdminUserService`
- 实现用户列表、新增、修改、删除
- 实现角色迁移
- 限制有提交记录的学生迁移或删除
- 限制最后一个管理员迁移或删除
- 限制当前登录管理员修改自己的角色
- 实现 CSV 用户导入
- CSV 支持逐行成功和逐行错误结果
- CSV 支持密码必填、用户名唯一、学号必填等规则

前端：

- 完成 `AdminUsers.vue`
- 用户列表、筛选、分页
- 新增和编辑弹窗
- 删除和角色迁移确认
- CSV 导入与导入结果展示
- 当前管理员和最后一个管理员危险操作禁用

### 3. 系统配置

数据与后端：

- 新增 `system_configs` 表
- 新增 `system_config_audit_logs` 表
- 新增配置实体、审计实体和 Repository
- 新增配置键枚举
- 新增 AI Provider 枚举
- 新增分组查询和更新 DTO
- 实现 `SystemConfigService`
- 新增 `AdminSystemConfigController`
- AI Key 使用 AES-GCM 加密
- 查询接口只返回 AI Key 掩码
- 支持设置和清除 AI Key
- 配置修改写入审计日志
- 配置表增加乐观锁
- 支持评测参数、AI 配置和限流阈值更新

前端：

- 完成 `AdminSystemConfig.vue`
- 评测参数表单
- AI 服务商、模型、Base URL 配置
- AI Key 状态和掩码展示
- 清除 AI Key 二次确认
- 全局、单用户、单 IP 限流阈值设置
- 保存后刷新最新配置

### 4. 业务配置读取与限流

- 新增 JudgeRuntimeConfig
- 新增 AiRuntimeConfig
- 新增 RateLimitRuntimeConfig
- JudgeService 每次触发评测时读取最新配置
- 新增 AiReviewService 配置读取占位实现
- 新增进程内 RateLimitService
- 登录接口接入限流
- 代码提交接口接入限流
- 超过阈值返回 HTTP 429

### 5. 操作日志 / 审计日志

后端：

- 新增 `operation_audit_logs` 表
- 新增操作审计实体和 Repository
- 新增 `@AuditOperation` 注解
- 新增 AOP 审计切面
- 新增异步审计写入服务
- 登录操作自动记录
- 用户新增、修改、删除自动记录
- 用户角色迁移自动记录
- CSV 导入自动记录
- 系统配置修改自动记录
- 密码、代码、AI Key、token 自动脱敏
- 新增审计日志查询接口
- 新增审计日志 CSV 导出接口

前端：

- 完成 `AdminAuditLogs.vue`
- 审计日志列表
- 用户名、模块、操作类型、状态筛选
- 时间范围筛选
- 分页
- 日志详情弹窗
- 当前筛选结果导出 CSV

### 6. 测试与安全评审

- 新增用户管理单元测试
- 新增 CSV 导入单元测试
- 新增真实 MySQL 集成测试
- 新增系统配置单元测试
- 新增 Controller 权限测试
- 新增 JudgeService 日志测试
- 新增 AiReviewService 日志安全测试
- 新增限流单元测试
- 新增 MockMvc 限流集成测试
- 当前后端测试数量：48
- 当前测试结果：全部通过
- 更新安全风险与加固建议文档

## 二、管理端尚未完成

### 高优先级

- [ ] 管理端工作台 `AdminHome.vue` 仍是占位页面
- [ ] 数据库监控页面尚未实现
- [ ] `/api/admin/db/check` 仅返回基础数据量，尚未返回连接数、版本、数据库大小、慢查询等指标
- [ ] 数据库监控告警规则尚未实现
- [ ] 数据库监控自动刷新尚未实现

### 中优先级

- [ ] 审计日志保留和定时清理尚未实现
- [ ] 审计日志详情页尚未做专门的权限测试
- [ ] 配置审计和业务审计的查询界面可以进一步统一
- [ ] 管理端页面缺少统一的公共组件和样式体系

### 后续增强

- [ ] JWT 密钥仍需改为生产环境变量
- [ ] 角色修改、删除后旧 token 尚未即时失效
- [ ] 登录接口目前只有单实例内存限流
- [ ] 多实例部署时需要改为 Redis 分布式限流
- [ ] 系统配置 AES-GCM 默认开发密钥需要在生产环境替换
- [ ] 操作审计异步队列需要考虑异常重试和持久化

## 三、当前可访问页面

```text
管理端工作台：
http://localhost:5173/admin/home

用户管理：
http://localhost:5173/admin/users

系统配置：
http://localhost:5173/admin/system-config

审计日志：
http://localhost:5173/admin/audit-logs
```

## 四、下一步建议

1. 完成数据库监控页面
2. 实现审计日志定时清理
3. 改造管理端工作台
4. 统一管理端表格、筛选和弹窗组件
5. 处理 JWT 失效和分布式限流等安全增强项
