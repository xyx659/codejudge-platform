# 2026-09-09 开发记录

> 日期：2026-09-09
> 记录范围：容器判题支持全部 Node 形态（图/树/链表）、新增 10 道测试题、序列化格式统一

## 一、今天已完成

### 1. 容器判题支持全部 Node 四种形态

**问题**：之前容器只能判普通类型（int/String/数组/ListNode/TreeNode），图（Node with neighbors）、N 叉树（Node with children）、随机指针链表（Node with random）、带 next 的二叉树（Node with left/right/next）四种 Node 形态全部报错「暂不支持的 Node 形态」。

**根因**：Node.java 包含所有字段（val/next/random/children/left/right/neighbors），`nodeKind()` 检测时 `random` 排第一，永远返回 `RANDOM_LIST` 而非正确形态。且 `DockerJudgeEngine` 没把辅助类传给 `generateMain`。

**修复**（`CodeRunner.java` + `DockerJudgeEngine.java`）：

- `DockerJudgeEngine`：先获取辅助类源码再传给 `generateMain`，使 Node 形态检测生效
- `hasAllNodeFields()`：判断 Node 类是否包含所有字段（无法区分形态时触发推断）
- `detectNodeKindFromSignature()`：从方法名/返回类型推断 Node 形态
  - `random`/`copyrandom` → RANDOM_LIST
  - `next`/`connect` → NEXT_TREE
  - `graph`/`clone` → GRAPH
  - 返回类型是 List（非 Node）→ NARY_TREE
  - 默认 → GRAPH
- Node 类 `children`/`neighbors` 默认初始化为 `new ArrayList<>()`，防止 NPE
- `parseClassFields` 正则支持 `= ...` 初始化语法

### 2. N 叉树解析/序列化重写

**问题**：`parseNaryTree` 按「每个节点后紧跟其子节点」格式解析，但 LeetCode 的 N 叉树序列化是「层序遍历，null 分隔每层」。

**修复**：
- `parseNaryTree`：改为层序解析，null 分隔每层，子节点按顺序分配给当前层的父节点
- `qlNaryTree`：序列化保持层序格式，null 分隔每层

### 3. NEXT_TREE 序列化 `#` 格式修复

**问题**：`qlNextTree` 在每层末尾都加 `#`，但 LeetCode 格式只在「有下一层」时加 `#`，最后一层不加。

**修复**：`qlNextTree` 只在 `!q.isEmpty()`（还有下一层）时才加 `#`。

### 4. 环形链表 `pos` 参数支持

**问题**：LeetCode 的 `hasCycle` 题目需要 `pos` 参数指定环入口位置（如 `head = [3,2,0,-4], pos = 1`），但 `parseListNode` 只能创建线性链表。

**修复**：
- 新增 `parseListNodeWithCycle(s, pos)`：先创建线性链表，再把尾节点的 `next` 指向第 `pos` 个节点
- 新增 `nextPosParam(p, currentIdx)`：从输入段中查找 `pos = X` 参数
- `parseExpr` 的 ListNode case 改为调用 `parseListNodeWithCycle`
- `qlListNode` 加入环检测（`visited` Set），避免环形链表序列化死循环

### 5. 新增 10 道 LeetCode 测试题

创建 `docs/leetcode-questions-2.json`，覆盖全部 Node 形态 + ListNode + TreeNode + int[]：

| # | 题目 | 类型 | 用例数 |
|---|------|------|--------|
| 1 | 复制带随机指针的链表 | RANDOM_LIST | 20 |
| 2 | N 叉树的层序遍历 | NARY_TREE | 20 |
| 3 | 填充每个节点的下一个右侧节点指针 | NEXT_TREE | 20 |
| 4 | N 叉树的前序遍历 | NARY_PRE | 20 |
| 5 | 填充每个节点的下一个右侧节点指针 II | NEXT_TREE_II | 20 |
| 6 | 环形链表 | ListNode + pos | 20 |
| 7 | 二叉树的最大深度 | TreeNode | 20 |
| 8 | 对称二叉树 | TreeNode | 20 |
| 9 | 最大子数组和 | int[] | 6 |
| 10 | 最长递增子序列 | int[] | 20 |

### 6. 序列化格式统一

- 数组/列表：统一为 LeetCode 无空格格式 `[0,1,2]`（不是 `[0, 1, 2]`）
- 树：层序格式 `[3,4,5,5,4,null,7]`
- 同步更新 `CodeRunnerTest` 和所有 JSON 测试用例的 `expected` 值

### 7. 全量验证结果

| 类型 | 题目 | 得分 | 用例 |
|------|------|------|------|
| GRAPH | 克隆图 | 100 | 5/5 |
| RANDOM_LIST | 复制带随机指针的链表 | 100 | 20/20 |
| NARY_TREE | N 叉树的层序遍历 | 100 | 20/20 |
| NEXT_TREE | 填充每个节点的下一个右侧节点指针 | 100 | 20/20 |
| NARY_PRE | N 叉树的前序遍历 | 100 | 20/20 |
| NEXT_TREE_II | 填充每个节点的下一个右侧节点指针 II | 100 | 20/20 |
| ListNode | 环形链表（支持 pos 参数） | 100 | 20/20 |
| TreeNode | 二叉树的最大深度 | 100 | 20/20 |
| TreeNode | 对称二叉树 | 100 | 20/20 |
| int[] | 最大子数组和 | 100 | 6/6 |
| int[] | 最长递增子序列 | 100 | 20/20 |

**总计：11 道题，186 个用例全部通过。**

## 二、改动文件

| 文件 | 改动 |
|------|------|
| `CodeRunner.java` | +183 行：Node 形态检测、N 叉树解析重写、NEXT_TREE 序列化修复、环形链表 pos 支持、序列化格式统一 |
| `DockerJudgeEngine.java` | +9 行：传递辅助类给 generateMain |
| `CodeRunnerTest.java` | 修改 expected 值（无空格格式） |
| `docs/leetcode-questions.json` | 更新 expected 值（无空格格式） |
| `docs/leetcode-questions-2.json` | 新增：10 道题 186 个测试用例 |

## 三、技术讨论

### 容器判题的数据类型维护成本

当前每新增一种类型需要改 3 处：`parseExpr` switch + `printLine` switch + `HELPERS_BASE` 静态方法。讨论了三种优化方案：

1. **TYPE_MAP 提取**：改成 2 处（Map + 静态方法），改动量从 3→2
2. **容器类型自动生成**：`List<T>`/`Map<K,V>`/`T[]` 等容器组合自动生成 parse/serialize，新增容器组合 0 改动
3. **JSON 通用序列化**：基础类型零改动，但自定义结构仍需手写

### 函数模式 vs 完整代码模式

讨论了如果让学生写完整代码（含 main 方法）的利弊：
- 优点：系统不需要 CodeRunner，新增类型零改动
- 缺点：学生要自己处理 I/O，错误定位更难（系统无法区分算法错误 vs I/O 错误）
- 结论：教学场景下函数模式的错误反馈质量更高，学生能更快定位问题

## 四、尚未完成

- [ ] 多语言支持（C/Python/C++）
- [ ] 评测结果 WebSocket/SSE 实时推送
- [ ] 容器类型自动生成优化（减少新增类型改动量）
- [ ] Redis 开机自启配置

## 五、下一步建议

1. 将今天新增的 10 道题与之前的 11 道题整合为完整题库
2. 优化容器类型 parse/serialize，减少新增类型改动量
3. 多语言评测（C/Python/C++）
